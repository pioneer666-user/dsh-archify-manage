# 实现方案：<业务名称>——<功能或改动名>

<!--
  用法提示（填写时可保留，交付前删除本注释块）：
  - 本模板与《实现段指引》（references/implementation.md）配套使用。
  - 「<…>」是占位符，交付前必须全部替换为真实内容；不适用的按需章节整节删除，不写「无」。
  - 本文件只解释"怎么实现、为什么这么选、影响哪里"；业务规则一律引用规格编号（FR-xxx），
    不在本文重新定义——规格没写清的回规格补或标【待裁决】。
  - 简单功能可以不落本文件：把开工三问（怎么实现 §2.1、影响哪里 §2.3、每步验收什么 §5）
    答清并获得作者同意即可（见指引 §1）。
-->

- 状态：<草案 / 已批准>
- 日期：<YYYY-MM-DD 创建；YYYY-MM-DD 最近修订>
- 规格：<业务规格文档路径>（本次涉及：<FR-xxx 清单>）
- 分支：<获批分支名>

## 1. 目标与范围

<!-- 一段话说清本次实现交付什么、明确不做什么；范围以作者批准为准，规则依据引用编号。 -->

## 2. 方案

### 2.1 怎么实现

<!-- 主要做法：动哪些模块、加什么机制、数据怎么流。写给看得懂代码的人，不贴大段代码。 -->

### 2.2 为什么这么选

<!--
  候选方案逐个说"做法—为什么没选"；没有像样的候选时写为什么这条路是显然的。
  关键设计决定按「决定／理由／放弃的替代」留痕，事后能追溯。
-->

### 2.3 影响面

<!--
  逐项列出会动到的文件、模块、接口，以及可能被波及的现有行为；
  涉及已有图／详情／证据的注明是否需要同步（同步流程见 references/change.md，先如实列出）。
-->

## 3. 技术上下文（按需）

<!--
  只填与本次实现相关、照项目现状能直接写的项；不展开成调研报告。
  有未定的技术前提标【待澄清】，集中问作者，不猜。
-->

- 语言／框架版本：
- 关键依赖：
- 存储与数据：
- 测试方式：<跑什么命令、看什么结果>
- 运行／部署环境：

## 4. 风险与回退（按需）

<!-- 可能出错的点、兼容性顾虑、出问题怎么退回；没风险就整节删除，不写"无"。 -->

## 5. 验证计划

<!--
  总览每一步拿什么证明它对了：测试命令、检查点、作者亲验项；
  业务逻辑与缺陷修复必须带必要的相关测试。逐任务的验收写在任务清单（或等价表达）里。
-->

- <验证项一：<命令／操作> → <预期结果>（覆盖 FR-xxx／AC-xxx）>
