# <业务名称>

<!--
  用法提示（填写时可保留，交付前删除本注释块）：
  - 本模板与《业务规格编写指引》（references/specification.md）配套使用。
  - 「<…>」是占位符，交付前必须全部替换为真实内容；不适用的按需章节整节删除，不写「无」。
  - 新业务的规则从 FR-001、验收从 AC-001 起编；已有业务沿用原编号，新增续编，不重编号。
-->

- 状态：<拟定设计 / 已裁决>
- 日期：<YYYY-MM-DD 创建；YYYY-MM-DD 最近修订>

## 目标

<!-- 全文唯一允许写成段落的地方：这个业务为谁解决什么问题。一到三句，不写做法。 -->

<这个业务为 <谁> 解决 <什么问题>；成功时 <有什么不同>。>

## 业务原则

<!-- 不可违反的底线，争议时用它裁决。只说"必须怎样/不得怎样"的立场，具体规则去「规则」节。 -->

- <原则一，如：已完成的操作不可自动撤销>
- <原则二，如：同一份数据同时只能有一个生效版本>

## 规则

<!--
  每条规则一个稳定编号，断言式写法，能判对错（"必须…"、"…不得…"）。
  规则在本节统一定义；验收、边界、场景可写具体条件与预期结果，但须引用规则编号，
  不新增或改变规则。
  尚未定案的写法：FR-004 <要求描述>【待裁决：<具体问题>】——不替裁决人拍板。
-->

- **FR-001**：<系统必须……>
- **FR-002**：<……不得……>
- **FR-003**：<要求描述>【待裁决：<具体问题与候选答案>】

## 边界与失败情况

<!--
  篇幅优先给这里：中断、重复、并发、越权、缺数据、数据坏了的时候，系统应该怎样。
  每条写「什么情况 → 应有什么结果」。结果若构成业务规则，规则编号进「规则」节、本条引用；
  本节不另立规则。顺利路径少写——它的文档价值低。
-->

- <重复提交同一请求 → 不得产生两次副作用（FR-00x）>
- <操作中途中断 → 重新进入能恢复到中断前状态（FR-00x）>
- <输入为空或格式不对 → 明确指出问题所在，不静默失败（FR-00x）>

## 验收

<!--
  什么算做到。验收判卷：让人能判断实现的对错，不是把实现步骤抄进来。
  两种条目均可：场景式（给定…当…那么…）与度量式（可核实的指标）；
  每条注明覆盖哪条规则编号；规则没写清的先回「规则」节补，不在验收里另立规则。
-->

- **AC-001**：给定 <条件>，当 <动作>，那么 <预期结果>（覆盖 FR-001）
- **AC-002**：<可度量的判据，如：整个流程 X 分钟内可完成>（覆盖 FR-00x）

## 场景与用法（按需）

<!--
  多角色、多步骤的复杂业务才保留本节；简单业务整节删除。
  每个场景一段：谁、在什么情况下做什么、完成后得到什么、为什么重要。
  场景不定义规则——规则去「规则」节编号，这里只叙述和引用。
-->

### 场景：<标题>（优先级 P1）

<谁>在<什么情况下><做什么>，完成后<得到什么>。
为什么重要：<一句话>。

**验收场景**：

1. 给定 <…>，当 <…>，那么 <…>（覆盖 FR-00x）

## 领域词汇（按需）

<!-- 同一个词在两处理解不同时保留本节；项目已有全局词汇表时优先引用，不重复定义。 -->

### <术语>

- **定义**：<这个词在本业务里精确指什么>
- **不是什么**：<排除容易混淆的含义，如：不是指 X，那是另一个词>

## 假设与默认（按需）

<!--
  编写者按行业惯例或上下文补的默认值，必须逐条列在这里，与已拍板的规则分开。
  事后被追认的：构成规则的更新对应 FR；澄清记录保留原话、时间与关联编号；
  其他决定更新相应正文。本节不残留已追认条目。
-->

- <假设：<填写内容与理由>（来源：行业惯例 / 上下文推断，尚未经业务方确认）>

## 澄清记录（按需：仅业务决策场合）

<!--
  仅在开发新功能、修改原有业务逻辑时保留本节；日常开发问答不记录。
  只记录影响业务的决策问题。每条标注三态之一：
  作者已拍板（原话＋时间戳）/ AI 建议 / 待裁决。原话逐字引用，宁可与分析重复。
-->

### <YYYY-MM-DD> · <主题>

- **问题**：<影响业务的决策问题>
- **作者决定**（<YYYY-MM-DD> 原话）：「<逐字引用>」→ 落点：<FR-00x（构成规则时更新「规则」节）／相应正文章节>
- 或 **AI 建议**（<YYYY-MM-DD>）：<建议内容，尚未经作者认可>
- 或 **待裁决**：<问题与候选答案，正文相应处已标【待裁决】>
